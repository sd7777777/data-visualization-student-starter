# Week 03 - First Visual

The first graphical representation is the interactive specialty scatterplot exported as `FirstVisual`. It uses D3 logarithmic position scales and a square-root radius scale, includes explicit units and median references, and supports pointer and keyboard inspection.
