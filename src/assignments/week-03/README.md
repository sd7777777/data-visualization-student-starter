# Week 03 - First Visual

The first graphical representation is the interactive specialty scatterplot exported as `FirstVisual` and displayed at `#week-3`. Its cost-and-volume view uses D3 logarithmic position scales; its drug-category view uses zero-based linear scales. Circle area uses a square-root scale, and every mark supports pointer and keyboard inspection.
